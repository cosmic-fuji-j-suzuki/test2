# 進捗表  

セッションを跨ぐ進捗ログ（追記式）。新しい記録を上に追記する。  

## 2026-07-05 フェーズ:要件定義（その3・確定）  
- 決めたこと:  
  - `user.03.md` の回答を反映し `ans.03.md`（要件定義確定版）を作成。  
    - 用語を「グループ」→「テナント」に全面統一（tenant_id）。  
    - ログインはメール一本化（グループ/テナントコード廃止）。  
    - 子どもも入力可。認証後に「ぼく・わたし／ぱぱ・まま」で分岐。  
    - 口座管理は既定OFF（単一口座）。  
    - 予算は費目別＋総額（任意）の両対応で確定。  
    - 2FA/QRの期限はsystem_settingsテーブルで変更可能に（初期値 2FA=10分, QR=30日）。  
    - 繰越は transaction_type の carryover で確定。  
    - 費目プリセット確定。テナント間費目ランキング取込は将来機能として記録。  
  - 次フェーズ P1-a（静的HTMLモック）の着手方針を策定。配置は 30.開発・実装/mock/。  
- 未決/宿題:  
  - `ans.03.md` の質問 I-1〜I-4 待ち（デザイン方向性、保護者確認要否、モック複数案の可否、メインカラー）。  
- 次アクション:  
  - `user.04.md` に回答 → P1-a ログイン画面モックを 2〜3 案で作成。  

## 2026-07-05 フェーズ:要件定義（その2）  
- 決めたこと:  
  - `user.02.md` の回答を反映し `ans.02.md` を作成。主要事項を確定:  
    - DB=PostgreSQL、モックは静的HTML（JSON見立て）から着手。  
    - AI相談/レシートOCRはダミー応答で先行実装。  
    - 子どもを専用の簡易ロールとして分離（残高中心・親が閲覧・昇格可）。  
    - 予算機能を追加。費目マスタは共通プリセット＋グループ別編集。  
    - 口座テーブル導入（UIは段階表示で複雑さを吸収、既定は単一口座案）。  
    - 繰越は transaction_type に carryover を追加する設計を推奨。  
    - ログインはメール一本化。QR/2FAはロール別ルールを仕様化。  
    - P1対象順: ログイン→ダッシュボード→出納帳→マスタメンテ。  
- 未決/宿題:  
  - `ans.02.md` の追加質問 G-1〜G-7 への回答待ち（グループコード廃止可否、子ども入力可否、口座既定OFF、総額予算、2FA/QR有効期限、繰越実装方針、費目過不足）。  
- 次アクション:  
  - `01.要件定義/user.03.md` に回答 → 反映して要件確定 → P1-a（静的HTMLモック）着手。  

## 2026-07-05 フェーズ:要件定義（その1）  
- 決めたこと:  
  - `user.01.md`（要件定義テンプレート記入版）を基に要件定義ドラフト `ans.01.md` を作成。  
  - 全体方針を整理：マルチテナント前提、段階的アプローチ（P1 モック→P2 設計→P3 開発→P4 テスト）。  
  - 技術たたき台を提示：Laravel＋Docker、DB は PostgreSQL 第一候補、初期は Blade + 少量 JS。  
- 未決/宿題:  
  - `ans.01.md` 末尾の質問 A〜E（全12問）への回答待ち。特に重要論点:  
    - 「無料」方針と AI/OCR 外部 API コストの競合（B-3, B-4）  
    - 予算機能の要否（C-6）／貯蓄のデータ設計（C-7）／残高繰越（C-9）  
    - 利用者ログイン ID 方式（D-10）  
- 次アクション:  
  - ユーザーが `01.要件定義/user.02.md` に回答を記入 → 反映して `ans.02.md` で要件を更新・確定。  
  - 要件確定後、P1（JSON モック）または `10.設計/` へ着手。  
